# Claude Boost — gitignore for .claude directory
# Managed by ualimxvp/claude-boost

# Audit logs (contain timestamps and command history)
logs/

# Local settings override (user-specific, not shared)
settings.local.json
